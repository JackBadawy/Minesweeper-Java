package com.main;

public class Main {
// I will make a seperate class for the array and rendering method and simply call it here
	public static void main(String[] args) {
		MineGrid mineGrid = new MineGrid(10, 10);
		Cell cell = mineGrid.getCell(0, 0);
		cell.setIntValue(5);
		cell.setCharValue('5');
		
		System.out.println("::ADDRESSES::");
		mineGrid.printMineGridAddressGrid();
		System.out.println("::PLAYER GRID::");
		mineGrid.printMineGrid();
		
		//todo todo add display for just chars
  // mineGrid.printMineGridPlayer();
// add way to input minefield address guess (scanner obviously).
		
	}
}

